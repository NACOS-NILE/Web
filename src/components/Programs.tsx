"use client";
import { useState } from "react"; import { programs } from "@/data/site";
export default function Programs(){const[active,setActive]=useState(0);return <section className="section programs" id="programs"><div className="page-shell"><p className="section-label light">03 / WHAT HAPPENS HERE</p><div className="program-heading"><h2>Learn beyond<br/><em>the lecture hall.</em></h2><p>Opportunities designed to turn curiosity into capability.</p></div><div className="program-layout"><div className="program-list">{programs.map((item,index)=><button type="button" aria-pressed={active===index} key={item.number} className={active===index?"active":""} onMouseEnter={()=>setActive(index)} onFocus={()=>setActive(index)} onClick={()=>setActive(index)}><span>{item.number}</span><strong>{item.title}</strong><p>{item.description}</p></button>)}</div><div className={`program-visual program-visual-${active}`} aria-hidden="true"><div className="visual-grid"/><span className="visual-code">MODULE / 0{active+1}</span><strong>{programs[active].title}</strong><div className="visual-node node-a"/><div className="visual-node node-b"/><div className="visual-node node-c"/><svg viewBox="0 0 100 100"><path d="M18 70 L48 30 L82 62"/><path d="M48 30 L67 78"/></svg><span className="visual-status">● SYSTEM ACTIVE</span></div></div></div></section>}
